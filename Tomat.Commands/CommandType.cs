namespace Tomat.CommandFramework
{
    /// <summary>
    ///     Enums indicating what type of command a given command is.
    ///     Used for categorization.
    /// </summary>
    public enum CommandType
    {
        Info,
        Fun,
        Hidden,
        Configuration
    }
}
